# Untuk Google Colab: torch, torchvision, numpy, matplotlib, scikit-learn, pillow,
# pyyaml biasanya SUDAH terpasang. Yang perlu diinstal manual hanya gradio & kaggle
# (notebook sudah menjalankan !pip install untuk keduanya).
#
# Daftar lengkap di bawah untuk menjalankan secara LOKAL (versi minimum yang aman).

torch>=2.0
torchvision>=0.15
scikit-learn>=1.3
pillow>=9.0
pyyaml>=6.0
matplotlib>=3.7
numpy>=1.24
gradio>=4.0
kaggle>=1.6
